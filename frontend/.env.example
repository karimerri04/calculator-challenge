# Development proxy target used by Vite.
# Change this when the Spring Boot backend runs on another port.
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Optional direct API base URL for deployments without the Vite proxy.
# Example: https://calculator.example.com
VITE_API_BASE_URL=
